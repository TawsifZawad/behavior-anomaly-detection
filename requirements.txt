# Core ML / data
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
joblib>=1.3
matplotlib>=3.7

# EVTX attack-sample reading (scripts/replay_attack_samples.py)
python-evtx>=0.7

# Windows live collection (collectors/windows_collector.py, USB monitor)
# Windows-only; ignored on Linux/macOS.
pywin32>=306 ; sys_platform == "win32"
WMI>=1.5 ; sys_platform == "win32"

# Packaging the standalone executable (see BUILD.md) — optional
# pyinstaller>=6.0
